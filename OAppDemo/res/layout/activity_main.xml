<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/VLinearFill"
    android:background="@color/background">
    
    <RelativeLayout style="@style/NavigationBar">
        <TextView style="@style/NavigationTitle"
            android:text="OApp Demo"/>
    </RelativeLayout>
    
    <ScrollView style="@style/Fill">
        <cn.o.app.ui.FlowLayout 
            style="@style/HFill"
            android:padding="@dimen/padding">
            
            <TextView 
                android:id="@+id/alert_by_activity"
                style="@style/MainBtn"
                android:text="Alert by Activity"/>
            
            <TextView
                android:id="@+id/alert"
                style="@style/MainBtn"
                android:text="Alert"/>
            
            <TextView
                android:id="@+id/oadapter"
                style="@style/MainBtn"
                android:text="OAdapter"/>
            
            <TextView
                android:id="@+id/zip"
                style="@style/MainBtn"
                android:text="Zip"/>
            
            <TextView
                android:id="@+id/conf_item"
                style="@style/MainBtn"
                android:text="ConfItem"/>
            
            <TextView
                android:id="@+id/aes"
                style="@style/MainBtn"
                android:text="AES"/>
            
            <TextView
                android:id="@+id/fir"
                style="@style/MainBtn"
                android:text="FIR"/>
            
            <TextView
                android:id="@+id/anim_task"
                style="@style/MainBtn"
                android:text="AnimTask"/>
            
            <TextView 
                android:id="@+id/media"
                style="@style/MainBtn"
                android:text="Media"/>
            
            <TextView
                android:id="@+id/net_task"
                style="@style/MainBtn"
                android:text="NetTask"/>
            
        </cn.o.app.ui.FlowLayout>
    </ScrollView>

</LinearLayout>
